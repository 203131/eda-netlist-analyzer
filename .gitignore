# ==============================================================================
# .gitignore - EDA Netlist Analyzer
# ==============================================================================

# CMake Out-of-source 編譯輸出目錄
build/

# 編譯產物 / 目的檔 / 執行檔
*.o
*.obj
*.exe
netlist_analyzer

# 編輯器 / IDE 設定
.vscode/

# 臨時產生的驗證用測試腳本（非正式測試套件的一部分，例如手動驗證 STA 用的網表）
tests/test.v

# 專案內部 AI Agent 規範文件，不納入版本控制
CLAUDE.md
